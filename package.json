{
  "name": "celeste-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --check app.js && node --check interaction-fix.js && node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8080"
  }
}
